\def\id{{\mathbf 1}}
\catcode`ı=\active \letı\id
\catcode`×=\active \let×\otimes

\foreach [count=\n] \o in
  {((w×x)×y)×x,
   (w×(x×y))×x,
   w×((x×y)×x),
   w×(x×(y×x)),
   (w×x)×(y×x)}
  \obj (\n) at ({72*\n:9em}) =\o{};

\mor* a_{w,x,y}×ı_z: 1 -> 2,
      a_{w,x×y,z}:   2 -> 3,
      ı_w×a_{x,y,z}: 3 -> 4,
      a_{w×x,y,z}:   1 -> 5,
      a_{w,x,y×z}:   5 -> 4;
