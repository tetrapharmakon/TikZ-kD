\def\id{{\mathbf 1}}

\catcode`ı=\active \letı\id
\catcode`×=\active \let×\otimes

\foreach [count=\n] \v in
  {((w×x)×y)×x,
   (w×(x×y))×x,
   w×((x×y)×x),
   w×(x×(y×x)),
   (w×x)×(y×x)}
  \obj (v\n) at ({72*\n:9em}) = \v;
\mor* ()=a_{w,x,y}×ı_z: v1 -> v2;
\mor* ()=a_{w,x×y,z}:   v2 -> v3;
\mor* ()=ı_w×a_{x,y,z}: v3 -> v4;
\mor* ()=a_{w×x,y,z}:   v1 -> v5;
\mor* ()=a_{w,x,y×z}:   v5 -> v4;
