\def\id{{\mathbf 1}}
\catcode`ı=\active \letı\id
\catcode`×=\active \let×\otimes

\foreach [count=\n] \o in
  {((w×x)×y)×x,
   (w×(x×y))×x,
   w×((x×y)×x),
   w×(x×(y×x)),
   (w×x)×(y×x)}
  \obj at=({72*\n:9em}),(\n):\o{};

\mor 1 a_{w,x,y}×ı_z:-> 2
         a_{w,x×y,z}:-> 3
       ı_w×a_{x,y,z}:-> 4;
\mor *   a_{w×x,y,z}:-> 5
         a_{w,x,y×z}:-> *;
